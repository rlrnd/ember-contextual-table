{{#if hasBlock}}
  {{yield (hash change=(action 'change') isRowSelected=(readonly isRowSelected) row=row rowIndex=rowIndex body=true disabled=disabled)}}
{{else}}
  {{input type='checkbox' disabled=disabled change=(action 'change' value='target.checked') checked=(readonly isRowSelected)}}
{{/if}}